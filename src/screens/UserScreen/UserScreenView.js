import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';
import { Text, View } from 'react-native';

class UserScreenView extends PureComponent {
  static propTypes = {};

  render() {
    return (
      <View>
        <Text>UserScreenView</Text>
      </View>
    );
  }
}

export default UserScreenView;
